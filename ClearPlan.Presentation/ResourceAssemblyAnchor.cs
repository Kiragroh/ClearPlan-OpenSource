namespace ClearPlan.Presentation
{
    /// <summary>
    /// Creates an explicit CLR reference from hosts that load shared XAML
    /// resources through a pack URI.
    /// </summary>
    public static class ResourceAssemblyAnchor
    {
        public static void EnsureLoaded()
        {
        }
    }
}
