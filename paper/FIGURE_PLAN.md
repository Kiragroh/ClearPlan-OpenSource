# ClearPlan Technical Note figure plan

| ID | Type | Description | Data source | Priority |
| --- | --- | --- | --- | --- |
| Figure 1 | Screenshot | Actual `mixed-review` simulator overview, captured at 1600 × 1000 px with the synthetic-use banner visible. | `ClearPlan.Simulator` deterministic capture | High |
| Figure 2 | Architecture | Editable vector diagram of source adapters, TPS-specific read-only adapters, the neutral review snapshot, and shared review/report outputs. | Repository architecture | High |
| Figure 3 | Two-panel comparison | Baseline versus mixed-review DVHs and status counts for the deterministic mixed scenario. | Checked-in scenario JSON | High |

Figure 1 is a real application capture, not a reconstructed mock-up. Figure 2
is generated as SVG, PDF, and PNG; SVG is the editable source. Figure 3 is
generated from checked-in JSON as SVG, PDF, and PNG. Captions are kept in the
manuscript rather than embedded in the figures.
